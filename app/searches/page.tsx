import { PlaceholderPage } from "@/components/features/page-shell";

export const metadata = { title: "Searches — Maintenance" };

export default function SearchesPage() {
  return <PlaceholderPage title="Searches" task="T1.4 (part-sourcing search history)" />;
}
