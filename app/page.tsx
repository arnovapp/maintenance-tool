export default function HomePage() {
  // Placeholder until T0.4 (app shell + nav) and T1.1 (command bar).
  // Kept intentionally bare to satisfy T0.1's acceptance: `pnpm dev` renders
  // a blank page with no errors.
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground font-mono text-sm">Maintenance</p>
    </main>
  );
}
