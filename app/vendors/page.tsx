import { PlaceholderPage } from "@/components/features/page-shell";

export const metadata = { title: "Vendors — Maintenance" };

export default function VendorsPage() {
  return <PlaceholderPage title="Vendors" task="T2.1 (vendor data model UI)" />;
}
